@model HomeViewModel


@await Html.PartialAsync("Carousel")

<h2>Vinyls of the week</h2>
<h4>Our weekly selection - just for you!</h4>

<div class="row">

    @foreach (var Vinyl in Model.VinylsOfTheWeek)
    {
        @Html.Partial("VinylOfTheWeekSummary", Vinyl)
    }

</div>